class bantam.Main {
    void main() {
	int[] array = new int[10];
	int n = 100;
	int i = 0;
	for (i = 0; i < 100; ++i) {
	    array[0]--;
	    --array[1];
	    print(++i);
	    print(n++);
	    int z = 10;
	    int j = 0;
	    int w = 5;
	    while (j < 10) {
		int a = 2;
		int b = 3;
		if (i >= 24) {
		    int c = 5;
		    break;
		}
		j = j + 1;
	    }
	    if (i >= 49) {
		int c = 4;
		break;
	    }
	}
	(new TextIO()).putInt(array[0]).putString("\n");
	(new TextIO()).putInt(array[1]).putString("\n");
	return;
    }
    
    void print(int n) {
	(new TextIO()).putInt(n).putString("\n");
	return;
    }
}
